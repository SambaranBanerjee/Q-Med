import React from 'react';
import ReactDOM from 'react-dom/client';
import Profiles from './Profile.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('r3') as HTMLElement).render(
    <React.StrictMode>
        <Profiles/>
    </React.StrictMode>
);