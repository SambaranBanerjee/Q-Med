import React from "react";
import ReactDOM from 'react-dom/client';
import SearchPage from "./SearchPage";
import './index.css';


ReactDOM.createRoot(document.getElementById('r6') as HTMLElement).render(
    <React.StrictMode>
        <SearchPage/>
    </React.StrictMode>
);